opentimestamps>=0.3.0,<0.5.0
# plyvel (2026-09-14): py-leveldb 0.201 does not compile past Python 3.11.
# Builds from source against the image's libleveldb-dev; no 3.13 wheel yet.
plyvel>=1.5.1
# Upstream's floor is 0.12.1, but the deployed otsd image pins 0.11.2
# (timestamp-gateway otsd/Dockerfile) and this code must stay compatible
# with it; floor widened to match deployed reality. stamper.py avoids
# CTransaction.calc_weight() (absent in 0.11.x) for this reason.
python-bitcoinlib>=0.11.2,<0.13.0
